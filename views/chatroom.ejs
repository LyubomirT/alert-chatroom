<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alert Chatroom</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <meta http-equiv="Content-Security-Policy" content="upgrade-insecure-requests">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/marked/4.0.0/marked.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@joeattardi/emoji-button@3.1.1/dist/index.min.js"></script>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/Alert.png" type="image/x-icon">
</head>
<body>
    <div class="chat-container">
        <div class="titlebar">
            <h1><i class="fas fa-comments"></i> <span id="room-name">Alert Chatroom</span></h1>
            <div class="search-container">
                <input type="text" id="search-input" placeholder="Search messages...">
                <button id="search-button"><i class="fas fa-search"></i></button>
            </div>
            <div>
                <button id="edit-room-name-btn" style="display: none;"><i class="fas fa-edit"></i> Edit Room Name</button>
                <button id="toggle-discoverability" style="display: none;"><i class="fas fa-eye"></i> Make Discoverable</button>
                <button id="leave-btn"><i class="fas fa-sign-out-alt"></i> Leave</button>
            </div>
        </div>
        <div id="search-results" class="search-results" style="display: none;"></div>
        <div class="chat-main">
            <div class="messages-container">
                <div id="messages"></div>
            </div>
            <div class="sidebar">
                <h2><i class="fas fa-info-circle"></i> Room Info</h2>
                <p><i class="fas fa-link"></i> Invite link: <textarea id="invite-link" readonly></textarea></p>
                <button id="toggle-invite-link" style="display: none;"><i class="fas fa-exchange-alt"></i> Toggle Link Type</button>
                <h3><i class="fas fa-users"></i> Online Users</h3>
                <ul id="users-list"></ul>
                <h3><i class="fas fa-ban"></i> Banned Users</h3>
                <ul id="banned-list"></ul>
            </div>
        </div>
        <div id="typing-indicator" style="display: none;"></div>
        <form id="message-form">
            <textarea id="message-input" placeholder="Type your message..." required></textarea>
            <label for="file-upload" class="file-upload-label">
                <i class="fas fa-file-upload"></i>
            </label>
            <input type="file" id="file-upload" accept="*/*" style="display: none;">
            <button type="submit"><i class="fas fa-paper-plane"></i> Send</button>
        </form>
    </div>
    <script>
        const roomId = "<%= roomId %>";
    </script>
    <script src="/js/client.js"></script>
</body>
</html>